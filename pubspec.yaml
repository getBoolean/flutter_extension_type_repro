name: flutter_extension_type_repro
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  test_package:
    path: packages/test_package
  hooks_riverpod: ^2.4.9
  riverpod_annotation: ^2.3.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  riverpod_generator: ^2.3.10
  build_runner: ^2.4.8

flutter:
  uses-material-design: true
